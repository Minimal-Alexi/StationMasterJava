import View.StationApplication;

/**
 * The Main class serves as the entry point for the StationMaster application.
 */
public class Main {

    /**
     * The main method which launches the StationMaster application.
     *
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        StationApplication.main(args);
    }
}